// Runtime deployment config. This file contains no secrets and can be replaced
// on the server without rebuilding Admin Web.
window.AI_TRANSLATOR_ADMIN_CONFIG = window.AI_TRANSLATOR_ADMIN_CONFIG || {
  backendUrl: "https://api.aitranova.com",
  allowBackendOverride: true,
  requireHttps: false,
};
