<div
  class="language-switcher"
  role="group"
  aria-label="Language"
>
  <button
    type="button"
    data-lang-choice="vi"
    class="language-button"
  >
    VI
  </button>

  <button
    type="button"
    data-lang-choice="en"
    class="language-button"
  >
    EN
  </button>
</div>

<style>
  .language-switcher {
    display: inline-flex;
    align-items: center;
    gap: 0;

    padding: 3px;

    border:
      1px solid
      rgba(164, 91, 255, 0.42);

    border-radius: 14px;

    background:
      rgba(21, 12, 34, 0.94);

    box-shadow:
      0 0 0 1px
      rgba(147, 92, 255, 0.08),
      0 8px 22px
      rgba(0, 0, 0, 0.26);

    overflow: hidden;
  }


  .language-button {
    min-width: 44px;
    height: 36px;

    padding: 0 12px;

    border: 0;
    border-radius: 10px;

    background: transparent;

    font-size: 11px;
    font-weight: 900;
    letter-spacing: 0.03em;
    line-height: 1;

    cursor: pointer;

    opacity: 1;

    transition:
      background 180ms ease,
      color 180ms ease,
      box-shadow 180ms ease,
      transform 180ms ease;
  }


  /* =======================================================
     ACTIVE
     ======================================================= */

  :global(
    html[data-language="vi"]
    [data-lang-choice="vi"]
  ),
  :global(
    html[data-language="en"]
    [data-lang-choice="en"]
  ) {
    background:
      linear-gradient(
        135deg,
        #9d1bd8 0%,
        #b329e9 48%,
        #d13cff 100%
      ) !important;

    color:
      #ffffff !important;

    box-shadow:
      0 0 18px
      rgba(195, 42, 255, 0.32),
      0 7px 18px
      rgba(94, 20, 153, 0.34),
      inset 0 1px 0
      rgba(255, 255, 255, 0.20) !important;

    transform:
      translateY(-1px);
  }


  /* =======================================================
     INACTIVE
     ======================================================= */

  :global(
    html[data-language="vi"]
    [data-lang-choice="en"]
  ),
  :global(
    html[data-language="en"]
    [data-lang-choice="vi"]
  ) {
    background:
      #ffffff !important;

    color:
      #24152f !important;

    box-shadow:
      inset 0 1px 0
      rgba(255, 255, 255, 0.85) !important;

    transform:
      none !important;
  }


  /* =======================================================
     HOVER — INACTIVE
     ======================================================= */

  :global(
    html[data-language="vi"]
    [data-lang-choice="en"]:hover
  ),
  :global(
    html[data-language="en"]
    [data-lang-choice="vi"]:hover
  ) {
    background:
      #f4ebfa !important;

    color:
      #6f208c !important;
  }


  /* =======================================================
     HOVER — ACTIVE
     ======================================================= */

  :global(
    html[data-language="vi"]
    [data-lang-choice="vi"]:hover
  ),
  :global(
    html[data-language="en"]
    [data-lang-choice="en"]:hover
  ) {
    filter:
      brightness(1.07);

    box-shadow:
      0 0 22px
      rgba(205, 51, 255, 0.42),
      0 9px 21px
      rgba(94, 20, 153, 0.38) !important;
  }


  .language-button:focus-visible {
    outline:
      2px solid
      #d58bff;

    outline-offset:
      2px;
  }
</style>
