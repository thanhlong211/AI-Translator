<div class="product-preview">

  <div class="preview-glow"></div>

  <div class="app-window">

    <!-- TITLE BAR -->

    <div class="titlebar">

      <div class="window-buttons">
        <i></i>
        <i></i>
        <i></i>
      </div>

      <div class="app-title">

        <span class="app-mark">
          A
        </span>

        <strong>
          AitraNova Desktop
        </strong>

      </div>

      <span class="desktop-label">
        Windows
      </span>

    </div>

    <!-- APP -->

    <div class="app-layout">

      <!-- SIDEBAR -->

      <aside class="app-nav">

        <div class="nav-brand">
          A
        </div>

        <button class="app-nav-item active">
          ✦
        </button>

        <button class="app-nav-item">
          漫
        </button>

        <button class="app-nav-item">
          文
        </button>

        <button class="app-nav-item">
          ↺
        </button>

        <div class="nav-space"></div>

        <button class="app-nav-item">
          ⚙
        </button>

      </aside>

      <!-- MAIN -->

      <div class="app-main">

        <div class="main-header">

          <div>

            <small>
              MANGA SESSION
            </small>

            <strong>
              Chapter 03 · Page 08
            </strong>

          </div>

          <div class="session-status">

            <span></span>

            Active

          </div>

        </div>

        <div class="main-content">

          <!-- MANGA -->

          <div class="manga-area">

            <div class="page-label">
              PAGE 08
            </div>

            <div class="manga-art">

              <div class="character">

                <div class="hair"></div>

                <div class="face"></div>

                <div class="body"></div>

              </div>

              <div class="bubble bubble-a">
                美咲！
              </div>

              <div class="bubble bubble-b">
                ここにいたのか？
              </div>

            </div>

          </div>

          <!-- TRANSLATION -->

          <div class="translation-area">

            <div class="translation-top">

              <div>

                <small>
                  TRANSLATION
                </small>

                <strong>
                  JA → VI
                </strong>

              </div>

              <span class="ai-pill">
                ✦ AI
              </span>

            </div>

            <div class="translation-card">

              <span class="source">
                美咲！
              </span>

              <strong>
                Misaki!
              </strong>

              <div class="continuity-pill">
                <span>✓</span>

                Character continuity
              </div>

            </div>

            <div class="translation-card">

              <span class="source">
                ここにいたのか？
              </span>

              <strong>
                Cậu ở đây sao?
              </strong>

            </div>

            <div class="context-panel">

              <span class="context-icon">
                ↺
              </span>

              <div>

                <small>
                  CONTEXT MEMORY
                </small>

                <strong>
                  4 previous pages
                </strong>

              </div>

              <span class="active-context">
                Active
              </span>

            </div>

          </div>

        </div>

      </div>

    </div>

  </div>

  <!-- FLOATING ITEMS -->

  <div class="floating floating-profile">

    <div class="float-icon purple">
      ✦
    </div>

    <div>

      <small>
        Translation Profile
      </small>

      <strong>
        Manga · Natural
      </strong>

    </div>

  </div>

  <div class="floating floating-memory">

    <div class="float-icon green">
      ✓
    </div>

    <div>

      <small>
        Character Memory
      </small>

      <strong>
        美咲 → Misaki
      </strong>

    </div>

  </div>

  <div class="shortcut-card">

    <span>
      Quick Translate
    </span>

    <kbd>Ctrl</kbd>

    <i>+</i>

    <kbd>Shift</kbd>

    <i>+</i>

    <kbd>Q</kbd>

  </div>

</div>

<style>
  .product-preview {
    position: relative;

    padding:
      22px 0 30px;
  }

  .preview-glow {
    position: absolute;

    top: 10%;
    left: 10%;

    width: 80%;
    height: 80%;

    border-radius: 50%;

    background:
      radial-gradient(
        circle,
        rgba(99, 91, 255, 0.18),
        transparent 68%
      );

    filter: blur(35px);

    pointer-events: none;
  }

  .app-window {
    position: relative;

    z-index: 2;

    overflow: hidden;

    border:
      1px solid
      rgba(218, 221, 234, 0.9);

    border-radius: 22px;

    background: white;

    box-shadow:
      0 35px 90px
      rgba(40, 43, 75, 0.16);
  }

  /* TITLE BAR */

  .titlebar {
    display: grid;

    height: 47px;

    grid-template-columns:
      1fr auto 1fr;

    align-items: center;

    padding: 0 15px;

    border-bottom:
      1px solid var(--border);

    background:
      rgba(255, 255, 255, 0.96);
  }

  .window-buttons {
    display: flex;

    gap: 6px;
  }

  .window-buttons i {
    width: 8px;
    height: 8px;

    border-radius: 50%;

    background: #d9dbe5;
  }

  .app-title {
    display: flex;

    align-items: center;

    gap: 7px;

    color: var(--text);

    font-size: 10px;
  }

  .app-mark {
    display: grid;

    width: 20px;
    height: 20px;

    place-items: center;

    border-radius: 6px;

    background:
      linear-gradient(
        135deg,
        var(--primary),
        var(--blue)
      );

    color: white;

    font-size: 8px;
    font-weight: 900;
  }

  .desktop-label {
    justify-self: end;

    color: var(--muted-2);

    font-size: 8px;
  }

  /* LAYOUT */

  .app-layout {
    display: grid;

    min-height: 430px;

    grid-template-columns:
      48px 1fr;
  }

  /* SIDEBAR */

  .app-nav {
    display: flex;

    flex-direction: column;

    align-items: center;

    gap: 10px;

    padding:
      14px 8px;

    border-right:
      1px solid var(--border);

    background:
      #f8f9fc;
  }

  .nav-brand {
    display: grid;

    width: 31px;
    height: 31px;

    place-items: center;

    margin-bottom: 8px;

    border-radius: 9px;

    background:
      linear-gradient(
        135deg,
        var(--primary),
        var(--blue)
      );

    color: white;

    font-size: 10px;
    font-weight: 900;
  }

  .app-nav-item {
    display: grid;

    width: 31px;
    height: 31px;

    padding: 0;

    place-items: center;

    border: 0;
    border-radius: 9px;

    background: transparent;

    color: var(--muted);

    font-size: 11px;
  }

  .app-nav-item.active {
    background:
      var(--primary-light);

    color:
      var(--primary);
  }

  .nav-space {
    flex: 1;
  }

  /* MAIN */

  .app-main {
    display: grid;

    min-width: 0;

    grid-template-rows:
      auto 1fr;
  }

  .main-header {
    display: flex;

    min-height: 58px;

    align-items: center;

    justify-content:
      space-between;

    padding:
      0 18px;

    border-bottom:
      1px solid var(--border);

    background:
      #fbfbfd;
  }

  .main-header small {
    display: block;

    color:
      var(--muted-2);

    font-size: 7px;

    font-weight: 800;

    letter-spacing:
      0.11em;
  }

  .main-header strong {
    display: block;

    margin-top: 3px;

    color: var(--text);

    font-size: 10px;
  }

  .session-status {
    display: flex;

    align-items: center;

    gap: 5px;

    color:
      var(--green);

    font-size: 8px;

    font-weight: 800;
  }

  .session-status span {
    width: 6px;
    height: 6px;

    border-radius: 50%;

    background:
      var(--green);

    box-shadow:
      0 0 0 4px
      rgba(36, 180, 126, 0.08);
  }

  .main-content {
    display: grid;

    grid-template-columns:
      minmax(0, 1.05fr)
      minmax(0, 0.95fr);
  }

  /* MANGA */

  .manga-area {
    position: relative;

    min-height: 370px;

    overflow: hidden;

    border-right:
      1px solid var(--border);

    background:
      linear-gradient(
        145deg,
        #edf0f6,
        #dde1eb
      );
  }

  .page-label {
    position: absolute;

    z-index: 4;

    top: 14px;
    left: 16px;

    padding:
      5px 7px;

    border-radius: 7px;

    background:
      rgba(255, 255, 255, 0.72);

    color:
      var(--muted);

    font-size: 7px;

    font-weight: 850;

    backdrop-filter:
      blur(8px);
  }

  .manga-art {
    position: absolute;

    inset: 0;
  }

  .character {
    position: absolute;

    right: 12%;
    bottom: -80px;

    width: 190px;
    height: 330px;
  }

  .hair {
    position: absolute;

    z-index: 1;

    top: 15px;
    left: 50%;

    width: 120px;
    height: 135px;

    transform:
      translateX(-50%);

    border-radius:
      48% 48% 42% 42%;

    background:
      #aeb4c4;
  }

  .face {
    position: absolute;

    z-index: 2;

    top: 34px;
    left: 50%;

    width: 82px;
    height: 100px;

    transform:
      translateX(-50%);

    border-radius:
      48% 48% 46% 46%;

    background:
      #d5d8e1;
  }

  .body {
    position: absolute;

    bottom: 0;
    left: 50%;

    width: 185px;
    height: 205px;

    transform:
      translateX(-50%);

    border-radius:
      55% 55% 0 0;

    background:
      #bac0cd;
  }

  .bubble {
    position: absolute;

    z-index: 5;

    padding:
      13px 17px;

    border:
      1.5px solid
      #4d5163;

    border-radius: 50%;

    background:
      rgba(255, 255, 255, 0.97);

    color:
      var(--text);

    font-size: 12px;

    font-weight: 800;

    box-shadow:
      0 8px 20px
      rgba(45, 49, 72, 0.06);
  }

  .bubble-a {
    top: 66px;
    right: 26px;
  }

  .bubble-b {
    bottom: 55px;
    left: 20px;

    max-width: 135px;

    text-align: center;
  }

  /* TRANSLATION */

  .translation-area {
    display: grid;

    align-content: start;

    gap: 11px;

    padding: 17px;

    background: white;
  }

  .translation-top {
    display: flex;

    align-items: center;

    justify-content:
      space-between;

    margin-bottom: 2px;
  }

  .translation-top small {
    display: block;

    color:
      var(--muted-2);

    font-size: 7px;

    font-weight: 800;

    letter-spacing:
      0.1em;
  }

  .translation-top strong {
    display: block;

    margin-top: 3px;

    color:
      var(--text);

    font-size: 10px;
  }

  .ai-pill {
    padding:
      5px 7px;

    border-radius: 7px;

    background:
      var(--primary-light);

    color:
      var(--primary);

    font-size: 8px;

    font-weight: 850;
  }

  .translation-card {
    padding: 14px;

    border:
      1px solid var(--border);

    border-radius: 12px;

    background:
      #fafbfe;
  }

  .source {
    display: block;

    margin-bottom: 6px;

    color:
      var(--muted);

    font-size: 9px;
  }

  .translation-card > strong {
    color:
      var(--text);

    font-size: 13px;
  }

  .continuity-pill {
    display: flex;

    width: fit-content;

    align-items: center;

    gap: 4px;

    margin-top: 8px;

    padding:
      4px 7px;

    border-radius: 999px;

    background:
      var(--primary-light);

    color:
      var(--primary);

    font-size: 7px;

    font-weight: 750;
  }

  .context-panel {
    display: flex;

    align-items: center;

    gap: 9px;

    margin-top: 1px;

    padding: 11px;

    border:
      1px solid var(--border);

    border-radius: 11px;

    background:
      linear-gradient(
        135deg,
        #f4f2ff,
        #f1f6ff
      );
  }

  .context-icon {
    display: grid;

    width: 29px;
    height: 29px;

    place-items: center;

    border-radius: 8px;

    background: white;

    color:
      var(--primary);

    box-shadow:
      var(--shadow-xs);
  }

  .context-panel div {
    flex: 1;
  }

  .context-panel small {
    display: block;

    color:
      var(--muted-2);

    font-size: 6px;

    letter-spacing:
      0.08em;
  }

  .context-panel strong {
    display: block;

    margin-top: 2px;

    color:
      var(--text);

    font-size: 9px;
  }

  .active-context {
    color:
      var(--green);

    font-size: 7px;

    font-weight: 800;
  }

  /* FLOAT */

  .floating {
    position: absolute;

    z-index: 10;

    display: flex;

    align-items: center;

    gap: 9px;

    padding:
      10px 12px;

    border:
      1px solid var(--border);

    border-radius: 13px;

    background:
      rgba(255, 255, 255, 0.95);

    box-shadow:
      0 18px 45px
      rgba(31, 36, 66, 0.13);

    backdrop-filter:
      blur(14px);
  }

  .floating small {
    display: block;

    color:
      var(--muted-2);

    font-size: 7px;
  }

  .floating strong {
    display: block;

    margin-top: 2px;

    color:
      var(--text);

    font-size: 9px;
  }

  .float-icon {
    display: grid;

    width: 29px;
    height: 29px;

    place-items: center;

    border-radius: 8px;

    font-size: 10px;

    font-weight: 900;
  }

  .float-icon.purple {
    background:
      var(--primary-light);

    color:
      var(--primary);
  }

  .float-icon.green {
    background:
      #eafaf3;

    color:
      var(--green);
  }

  .floating-profile {
    top: 90px;
    left: -44px;
  }

  .floating-memory {
    right: -36px;
    bottom: 64px;
  }

  .shortcut-card {
    position: absolute;

    z-index: 10;

    right: 20px;
    bottom: 0;

    display: flex;

    align-items: center;

    gap: 5px;

    padding:
      9px 11px;

    border:
      1px solid var(--border);

    border-radius: 11px;

    background: white;

    box-shadow:
      var(--shadow-sm);

    color:
      var(--muted);

    font-size: 8px;
  }

  .shortcut-card > span {
    margin-right: 4px;

    color:
      var(--text);

    font-weight: 750;
  }

  .shortcut-card i {
    font-style: normal;
  }

  kbd {
    padding:
      4px 6px;

    border:
      1px solid
      var(--border-strong);

    border-radius: 5px;

    background:
      #f8f9fc;

    color:
      var(--text);

    font-size: 7px;

    box-shadow:
      0 1px 0
      var(--border-strong);
  }

  @media (max-width: 700px) {
    .app-layout {
      grid-template-columns:
        42px 1fr;
    }

    .main-content {
      grid-template-columns:
        1fr;
    }

    .translation-area {
      display: none;
    }

    .manga-area {
      border-right: 0;
    }

    .floating {
      display: none;
    }
  }
</style>
