# Desktop development defaults.
# Copy this file to .env only if your local scripts load a root .env file.
# Never put real production secrets in this example file.

AI_TRANSLATOR_BACKEND_URL=http://localhost:8080

# Backend secrets/configuration should be supplied through environment variables
# or a local application-local.properties file that is ignored by Git.
